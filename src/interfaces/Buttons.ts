export interface button {
  status: string;
  value: number|string;
}

export const add: Function = (a: number, b: number): number => a + b;
export const multiply: Function = (a: number, b: number): number => a + b;
export const sub: Function = (a: number, b: number): number => a - b;
export const div: Function = (a: number, b: number): number => a / b;

